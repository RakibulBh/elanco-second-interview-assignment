export type Country = {
  name: string;
  currency: string;
  capital: string;
  flag: string;
  unicodeFlag: string;
  population: number;
};

export type Population = {
  year: number;
  value: number;
};
